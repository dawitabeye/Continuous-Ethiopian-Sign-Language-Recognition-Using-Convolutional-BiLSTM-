# Linux Password Reset Steps (Offline Method using Live CD or Recovery Mode)

# 1. List block devices to identify root disk
lsblk

# 2. Create a mount point
mkdir /mnt/reset

# 3. Activate all volume groups
vgchange -ay

# 4. Scan and identify logical volumes
lvscan

# 5. Mount the root logical volume (adjust device path as needed)
mount /dev/mapper/pve-root /mnt/reset

# 6. Change root into the mounted system
chroot /mnt/reset

# 7. Reset the password for the target user
passwd dbuadmin
# Enter new password when prompted

# 8. Exit chroot environment
exit

# 9. Unmount the system
umount /mnt/reset

# 10. Reboot the system
reboot
